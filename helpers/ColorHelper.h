#pragma once

namespace helpers {
    /**
     * Based on https://quasar.dev/style/color-palette#color-list
     * Default color tint level is 6, dark is 9 and light is 3.
     */
    const auto colorAmber = ftxui::Color::RGB(255, 193, 7);
    const auto colorAmberDark = ftxui::Color::RGB(255, 143, 0);
    const auto colorAmberLight = ftxui::Color::RGB(255, 224, 130);
    const auto colorBlue = ftxui::Color::RGB(63, 81, 181);
    const auto colorBlueDark = ftxui::Color::RGB(40, 53, 147);
    const auto colorBlueLight = ftxui::Color::RGB(159, 168, 218);
    const auto colorBlueGrey = ftxui::Color::RGB(96, 125, 139);
    const auto colorBlueGreyDark = ftxui::Color::RGB(55, 71, 79);
    const auto colorBlueGreyLight = ftxui::Color::RGB(176, 190, 197);
    const auto colorBrown = ftxui::Color::RGB(121, 85, 72);
    const auto colorBrownDark = ftxui::Color::RGB(78, 52, 46);
    const auto colorBrownLight = ftxui::Color::RGB(188, 170, 164);
    const auto colorCyan = ftxui::Color::RGB(0, 188, 212);
    const auto colorCyanDark = ftxui::Color::RGB(0, 131, 143);
    const auto colorCyanLight = ftxui::Color::RGB(128, 222, 234);
    const auto colorDeepOrange = ftxui::Color::RGB(255, 87, 34);
    const auto colorDeepOrangeDark = ftxui::Color::RGB(216, 67, 21);
    const auto colorDeepOrangeLight = ftxui::Color::RGB(255, 171, 145);
    const auto colorDeepPurple = ftxui::Color::RGB(103, 58, 183);
    const auto colorDeepPurpleDark = ftxui::Color::RGB(69, 39, 160);
    const auto colorDeepPurpleLight = ftxui::Color::RGB(179, 157, 219);
    const auto colorGreen = ftxui::Color::RGB(76, 175, 80);
    const auto colorGreenDark = ftxui::Color::RGB(46, 125, 50);
    const auto colorGreenLight = ftxui::Color::RGB(165, 214, 167);
    const auto colorGrey = ftxui::Color::RGB(158, 158, 158);
    const auto colorGreyDark = ftxui::Color::RGB(66, 66, 66);
    const auto colorGreyLight = ftxui::Color::RGB(238, 238, 238);
    const auto colorIndigo = ftxui::Color::RGB(63, 81, 181);
    const auto colorIndigoDark = ftxui::Color::RGB(40, 53, 147);
    const auto colorIndigoLight = ftxui::Color::RGB(159, 168, 218);
    const auto colorLightBlue = ftxui::Color::RGB(3, 169, 244);
    const auto colorLightBlueDark = ftxui::Color::RGB(2, 119, 189);
    const auto colorLightBlueLight = ftxui::Color::RGB(129, 212, 250);
    const auto colorLightGreen = ftxui::Color::RGB(139, 195, 74);
    const auto colorLightGreenDark = ftxui::Color::RGB(85, 139, 47);
    const auto colorLightGreenLight = ftxui::Color::RGB(197, 225, 165);
    const auto colorLime = ftxui::Color::RGB(205, 220, 57);
    const auto colorLimeDark = ftxui::Color::RGB(158, 157, 36);
    const auto colorLimeLight = ftxui::Color::RGB(230, 238, 156);
    const auto colorOrange = ftxui::Color::RGB(255, 152, 0);
    const auto colorOrangeDark = ftxui::Color::RGB(239, 108, 0);
    const auto colorOrangeLight = ftxui::Color::RGB(255, 204, 128);
    const auto colorPink = ftxui::Color::RGB(233, 30, 99);
    const auto colorPinkDark = ftxui::Color::RGB(173, 20, 87);
    const auto colorPinkLight = ftxui::Color::RGB(244, 143, 177);
    const auto colorPurple = ftxui::Color::RGB(156, 39, 176);
    const auto colorPurpleDark = ftxui::Color::RGB(106, 27, 154);
    const auto colorPurpleLight = ftxui::Color::RGB(206, 147, 216);
    const auto colorRed = ftxui::Color::RGB(244, 67, 54);
    const auto colorRedDark = ftxui::Color::RGB(198, 40, 40);
    const auto colorRedLight = ftxui::Color::RGB(239, 154, 154);
    const auto colorTeal = ftxui::Color::RGB(0, 150, 136);
    const auto colorTealDark = ftxui::Color::RGB(0, 105, 92);
    const auto colorTealLight = ftxui::Color::RGB(128, 203, 196);
    const auto colorYellow = ftxui::Color::RGB(255, 235, 59);
    const auto colorYellowDark = ftxui::Color::RGB(249, 168, 37);
    const auto colorYellowLight = ftxui::Color::RGB(255, 245, 157);

    const auto colorPrimary = ftxui::Color::RGB(25, 118, 210);
    const auto colorSecondary = ftxui::Color::RGB(38, 166, 154);
    inline const auto& colorAccent = colorPurple;
    const auto colorDark = ftxui::Color::RGB(29, 29, 29);
    const auto colorPositive = ftxui::Color::RGB(33, 186, 69);
    const auto colorNegative = ftxui::Color::RGB(193, 0, 21);
    const auto colorInfo = ftxui::Color::RGB(49, 204, 236);
    const auto colorWarning = ftxui::Color::RGB(242, 192, 55);
}
